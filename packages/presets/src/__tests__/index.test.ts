/**
 * Presets package tests
 */

import { describe, it, expect } from 'vitest';

describe('Presets', () => {
  it('should export preset functionality', () => {
    // Basic test to ensure the package works
    expect(true).toBe(true);
  });
});
